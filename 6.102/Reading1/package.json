{
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.2.x"
  },
  "scripts": {
    "cumulative-avg": "tsc && node -r source-map-support/register dist/cumulative-avg.js",
    "check": "tsc && node -r source-map-support/register dist/check.js"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "source-map-support": "^0.5.21"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
